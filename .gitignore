# Dependencies
node_modules/

# Model-pipeline intermediates — regenerate with packages/embed tools
*.raw.glb

# Demo build output — regenerate with `npm run build` / `build:standalone`
apps/demo/embed.js
apps/demo/embed.css
apps/demo/standalone.html

# The embed bundle the SERVICE serves. Built inside packages/api/Dockerfile,
# so the repository never needs a checked-in copy.
packages/embed/dist/

# OS/editor
.DS_Store
.vscode/
.idea/
packages/studio/dist-cloud/
