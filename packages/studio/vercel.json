{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "comment": "/p/<id> and /signin are client routes, not files. Anything that is not a built asset returns the app shell.",
      "source": "/((?!assets/).*)",
      "destination": "/index.html"
    }
  ]
}
