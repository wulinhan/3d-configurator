{
  "name": "@allin/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The service behind the Studio: accounts, saved projects, published versions, customer uploads",
  "main": "src/main.ts",
  "scripts": {
    "start": "node --experimental-strip-types src/main.ts",
    "test": "npm run test:unit",
    "test:unit": "node --test --experimental-strip-types \"test/*.test.ts\""
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.700.0",
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.5.4"
  }
}
